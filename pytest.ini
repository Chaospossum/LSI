[pytest]
testpaths = tests
filterwarnings =
    # third-party import noise only; nothing here is raised by this project's code
    ignore::DeprecationWarning:geopandas.*
    ignore::DeprecationWarning:pyproj.*
    ignore:.*matmul.*:PendingDeprecationWarning:rasterio.*
